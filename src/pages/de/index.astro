---
import { getCollection } from 'astro:content';

import Layout from '@layouts/Layout.astro';
import PageIndex from '@components/PageIndex.astro';

const [page] = await getCollection('pageHomeDe');
---

<Layout>
  <PageIndex page={page} />
</Layout>
